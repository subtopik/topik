{
  "name": "@topik/workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "changeset": "changeset",
    "ready": "vp fmt && vp lint && vp run -r test && vp run -r build",
    "dev": "vp run dev",
    "prepare": "vp config",
    "verify:asset-packages": "node tools/verify-asset-packages.mjs"
  },
  "devDependencies": {
    "@changesets/cli": "catalog:",
    "@types/node": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.30.0"
}
